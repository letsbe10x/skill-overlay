# l10x Post-run: lets-research-prd-grooming

## Inspect role execution packets

After `l10x research prd groom` completes, inspect `role_execution_packets.json`. If it contains open packets, run or dispatch those bounded roles before promoting the run as complete:

```bash
cat role_execution_packets.json
```

Each packet should include doctrine refs, a role playbook, persona lenses, thinking archetypes, question banks, and rubrics. Do not claim agentic completion until all open packets have been fulfilled by a validated `role_outputs` payload and a follow-up run.
