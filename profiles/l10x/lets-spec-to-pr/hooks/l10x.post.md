# l10x Post-run: lets-spec-to-pr

## Release run lock after PR creation

After the PR is created with `gh pr create`, release the workspace lock:

```bash
l10x run release $RUN_ID
```

Run store entries for each `change_code` and the final `verify_change` run enable traceability from spec requirement to code change to verification result.
